# Normalize line endings in the repository; check out native on Windows.
* text=auto

# Fixtures and golden files are compared byte for byte, so they must never be rewritten.
fixtures/** text eol=lf
tests/snapshots/** text eol=lf
tests/golden/** text eol=lf
evals/*.jsonl text eol=lf
