# Generated by Trace2Evals - do not edit by hand.
#
# version: v1   cases: 5
# Regenerate with: t2e export --version v1 --format promptfoo
#
# CAVEAT: Promptfoo evaluates the final output text. It does not see the agent's tool trace, so
# `must_call_tool` and `must_escalate` are approximated by inspecting what the output *says*. Those
# two assertions are strictly weaker here than in the generated pytest suite, which checks the real
# trace. Treat this file as a convenience for prompt iteration, not as the authoritative eval gate.

description: Trace2Evals suite v1
prompts:
- '{{input}}'
providers: []
tests:
- description: v1-0001
  vars:
    input: Bank transaction BNK-20260814-0042 for 4,812.50 GBP has no matching GL
      entry. Investigate and propose a root cause with evidence.
  assert:
  - type: javascript
    value: output.includes("get_invoice")
  - type: javascript
    value: (output.match(/\b[A-Z]{2,6}-[A-Z0-9]{2,}(?:-[A-Z0-9]+)*\b/g) || []).length
      >= 1
  metadata:
    tags:
    - wrong_tool
    - ungrounded
    - source:otel
- description: v1-0002
  vars:
    input: Transaction BNK-20260814-0067 for 19,400.00 USD references PO-99001, which
      does not exist in the ledger. Investigate.
  assert:
  - type: javascript
    value: output.toLowerCase().includes("escalate") || output.toLowerCase().includes("escalated")
      || output.toLowerCase().includes("escalating") || output.toLowerCase().includes("refer
      to a human") || output.toLowerCase().includes("human review") || output.toLowerCase().includes("needs
      review") || output.toLowerCase().includes("cannot determine") || output.toLowerCase().includes("insufficient
      evidence") || output.toLowerCase().includes("flag_exception") || output.toLowerCase().includes("escalate")
      || output.toLowerCase().includes("escalate_to_human") || output.toLowerCase().includes("create_ticket")
      || output.toLowerCase().includes("notify_reviewer") || output.toLowerCase().includes("request_approval")
  - type: icontains-all
    value:
    - PO-99001
  metadata:
    tags:
    - no_escalation
    - hallucinated_fact
    - had_error
    - source:otel
- description: v1-0003
  vars:
    input: Reconcile the 2026-08 intercompany balance between entity GB01 and entity
      US04.
  assert:
  - type: is-json
    value:
      properties:
        evidence:
          items:
            type: string
          minItems: 1
          type: array
        root_cause:
          type: string
      required:
      - root_cause
      - evidence
      type: object
  metadata:
    tags:
    - format_break
    - had_error
    - source:langsmith
- description: v1-0004
  vars:
    input: Accrual reversal AC-2026-0311 for 4,120.00 GBP did not reverse in 2026-08.
      Route to the right specialist and resolve.
  assert:
  - type: regex
    value: \bSCHED-1188\b
  - type: javascript
    value: output.includes("flag_exception")
  metadata:
    tags:
    - source:langsmith
    - multi_agent
- description: v1-0005
  vars:
    input: 'A hand-written case: a 3.00 GBP variance below the 100.00 materiality
      threshold.'
  assert:
  - type: regex
    value: (?i)below the .*threshold
  metadata:
    tags:
    - manual
    - materiality
